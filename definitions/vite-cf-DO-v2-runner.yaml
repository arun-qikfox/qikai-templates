name: "vite-cf-DO-v2-runner"
description: "Vite/React full-stack template with pluggable data store (Firestore default)"
base_reference: "vite-reference"

# Patch package.json to match original template name
package_patches:
  name: "vite-cf-do-v2-runner"

excludes:
  - "worker/userRoutes.ts"
  