name: "vite-cf-DO-KV-runner"
description: "Vite/React application with seeded Firestore datastore and HTTP provider fallback"
base_reference: "vite-reference"

# Patch package.json to match original template name
package_patches:
  name: "vite-cf-do-kv-runner"
